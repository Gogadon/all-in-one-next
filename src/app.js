import{MODULES,getModule}from'./modules.js';import{todayIso,formatMetric,completedSessions,statistics,esc,shiftPeriod,formatNumber,emptyState,parseNumber}from'./core.js';import{loadStore,getState,subscribe,updateState,replaceState}from'./store.js';import{overview,editorView,detail,newTour,editTour,cancelEdit,setTitle,setMetric,saveTour,deleteTour}from'./tours.js';import{view as challengeView,addChallenge,removeChallenge}from'./challenges.js';import{exportJson,importJson}from'./storage.js';const app=document.querySelector('#app'),file=document.querySelector('#importFile');let statType='month',statAnchor=todayIso();const route=()=>{const p=(location.hash.slice(1)||'/dashboard').split('/').filter(Boolean);return{section:p[0]||'dashboard',moduleId:p[0]==='module'?p[1]:null,view:p[0]==='module'?(p[2]||'overview'):null,id:p[3]||null}};const nav=p=>location.hash=p.startsWith('/')?p:'/'+p;const toast=m=>{document.querySelector('.toast')?.remove();const e=document.createElement('div');e.className='toast';e.textContent=m;document.body.append(e);setTimeout(()=>e.remove(),3200)};const safe=async f=>{try{await f()}catch(e){console.error(e);toast(e.message||String(e))}};
function shell(title,content,{module=null,back=false,bottom=''}={}){return`<div class="shell" style="--accent:${module?.color||'#F6F8FA'}"><header class="topbar"><div>${back?'<button class="icon" data-action="back">←</button>':''}</div><strong>${esc(title)}</strong><div><button class="icon" data-action="settings">⚙</button></div></header><main class="content">${content}</main>${bottom}</div>`}
function dashboard(){const cards=MODULES.map(m=>{let meta=m.type==='tour'?`${completedSessions(getState(),m.id).length} ${m.plural}`:m.type==='challenge'?`${getState().challenges.length} Ziele`:'Engine vorbereitet';return`<button class="module" style="--m:${m.color}" data-action="module" data-module="${m.id}"><span>${m.icon}</span><b>${esc(m.label)}</b><small>${esc(meta)}</small></button>`}).join('');const d=new Date().toLocaleDateString('de-DE',{weekday:'long',day:'numeric',month:'long'});return`<section class="hero"><span class="eyebrow">All-in-One Next</span><h1>Dein Training.<br>Deine Touren.</h1><p>${esc(d)}</p></section><div class="grid">${cards}</div>`}
function statsView(m){const r=statistics(getState(),m.id,statType,statAnchor),items=Object.entries(r.metrics).length?Object.entries(r.metrics).map(([t,v])=>`<div class="card"><small>${esc(t)}</small><div class="big">${esc(formatMetric(t,v))}</div></div>`).join(''):'<div class="card empty">Keine Daten in diesem Zeitraum.</div>';return`<section class="hero"><span class="eyebrow">${esc(m.label)}</span><h1>Statistik</h1><p>${esc(statAnchor)}</p></section><div class="periods">${[['week','Woche'],['month','Monat'],['year','Jahr']].map(([x,l])=>`<button class="${x===statType?'active':''}" data-action="stat.type" data-type="${x}">${l}</button>`).join('')}</div><div class="grid top"><button class="button" data-action="stat.shift" data-step="-1">← zurück</button><button class="button" data-action="stat.shift" data-step="1">vor →</button></div><div class="stats top">${items}</div>`}
function settings(){return`<section class="hero"><span class="eyebrow">App</span><h1>Einstellungen</h1></section><div class="settings"><button class="button" data-action="backup.export">Backup exportieren</button><button class="button" data-action="backup.import">Backup importieren</button><button class="button danger" data-action="reset">Alle Daten löschen</button></div>`}
function render(){const r=route();if(r.section==='dashboard'){app.innerHTML=shell('All-in-One',dashboard());return}if(r.section==='settings'){app.innerHTML=shell('Einstellungen',settings(),{back:true});return}const m=getModule(r.moduleId);if(!m){nav('/dashboard');return}let c='',bottom='';if(m.type==='tour'){if(r.view==='edit')c=editorView(m);else if(r.view==='detail')c=detail(getState(),m,r.id);else if(r.view==='statistics')c=statsView(m);else c=overview(getState(),m);bottom=`<nav class="bottom"><button class="${r.view==='overview'?'active':''}" data-action="mview" data-view="overview">Touren</button><button class="${r.view==='statistics'?'active':''}" data-action="mview" data-view="statistics">Statistik</button></nav>`}else if(m.type==='challenge')c=challengeView(getState(),todayIso());else c=`<section class="hero"><span class="eyebrow">Kraft</span><h1>Kraftmodul</h1></section><div class="card"><h2>Fundament steht</h2><p>Übungen, Sätze, Plan, Zyklus und Progression werden hier als eigene Engine ergänzt.</p></div>`;app.innerHTML=shell(m.label,c,{module:m,back:r.view==='edit'||r.view==='detail',bottom})}
document.addEventListener('click',e=>{const el=e.target.closest('[data-action]');if(!el)return;safe(async()=>{const a=el.dataset.action,r=route(),m=getModule(r.moduleId);if(a==='back')history.back();else if(a==='settings')nav('/settings');else if(a==='module')nav(`/module/${el.dataset.module}/overview`);else if(a==='mview')nav(`/module/${r.moduleId}/${el.dataset.view}`);else if(a==='tour.new'){newTour(m);nav(`/module/${m.id}/edit`)}else if(a==='tour.open')nav(`/module/${m.id}/detail/${el.dataset.id}`);else if(a==='tour.edit'){const s=getState().sessions.find(x=>x.id===el.dataset.id);editTour(s);nav(`/module/${m.id}/edit`)}else if(a==='tour.cancel'){cancelEdit();history.back()}else if(a==='tour.save'){await updateState(s=>saveTour(s),{snapshot:true,reason:'before-tour-save'});toast('Gespeichert ✓');nav(`/module/${m.id}/overview`)}else if(a==='tour.delete'){if(confirm('Tour wirklich löschen?')){await updateState(s=>deleteTour(s,el.dataset.id),{snapshot:true,reason:'before-delete'});nav(`/module/${m.id}/overview`)}}else if(a==='stat.type'){statType=el.dataset.type;render()}else if(a==='stat.shift'){statAnchor=shiftPeriod(statType,statAnchor,Number(el.dataset.step));render()}else if(a==='challenge.add'){const type=document.querySelector('#challengeType').value,target=parseNumber(document.querySelector('#challengeTarget').value),period=document.querySelector('#challengePeriod').value;if(!target||target<=0)throw Error('Gültigen Zielwert eintragen.');await updateState(s=>addChallenge(s,{type,target,period}))}else if(a==='challenge.delete')await updateState(s=>removeChallenge(s,el.dataset.id));else if(a==='backup.export'){const blob=new Blob([exportJson(getState())],{type:'application/json'}),url=URL.createObjectURL(blob),x=document.createElement('a');x.href=url;x.download=`all-in-one-next-${todayIso()}.json`;x.click();URL.revokeObjectURL(url)}else if(a==='backup.import')file.click();else if(a==='reset'){if(confirm('Wirklich alle Daten löschen?')){await replaceState(emptyState());nav('/dashboard')}}})});
document.addEventListener('change',e=>{const el=e.target.closest('[data-change]');if(!el)return;const m=getModule(route().moduleId);if(el.dataset.change==='tour.title')setTitle(el.value);if(el.dataset.change==='tour.metric')setMetric(m,el.dataset.type,el.value)});file.addEventListener('change',()=>safe(async()=>{const f=file.files?.[0];if(!f)return;await replaceState(importJson(await f.text()));file.value='';toast('Backup importiert ✓');nav('/dashboard')}));window.addEventListener('hashchange',render);subscribe(render);await loadStore();if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js').catch(console.warn);render();